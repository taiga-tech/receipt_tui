# よく使うコマンド
- フォーマット: `mise run fmt`
- フォーマット検証: `mise run fmt-check`
- Lint: `mise run lint` (clippy, warnings as errors)
- 実行: `cargo run` (repo 直下に `credentials.json` が必要)
- 型チェック: `cargo check`
- テスト: `cargo test`
- 便利コマンド (Darwin): `ls`, `rg`/`grep`, `find`, `git`, `cat`
